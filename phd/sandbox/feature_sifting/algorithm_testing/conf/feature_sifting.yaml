project: feature-sifting
device: gpu
jax_jit_cache_dir: /tmp/jax_cache

# Experiment tracking
wandb: false
mlflow: false
comet_ml: false

# Task (feature sifting problem) — passed straight to FeatureSiftingTask
task:
  n_target_features: 10
  n_learner_features: 20
  target_noise_std: 0.0
  flip_period: -1

# Method (algorithm under test). Hyperparameter defaults live in the method's
# own file; override them from the CLI with e.g.
#   +method.hparams.learning_rate=0.001
method:
  name: lms # {lms, cbp_autostep, hybrid}
  hparams: {}

# Training
train:
  total_steps: 100_000
  log_freq: 1000      # steps per scan == steps aggregated per logged point

# Seed: single int, a list of ints (run in parallel over seeds via vmap), or null (random)
seed: null
