experiment: paper-weight-pruning-scaling-sweep
sweep_name: 02b_dense_scaling_4task_nonstationary_lr
output_dir: outputs
optuna_storage: ${oc.env:OPTUNA_TRACKING_URI}
mlflow_storage: ${oc.env:MLFLOW_TRACKING_URI}
command: python experiments/train_weight_pruning.py --config-path=../conf/sweeps/weight_pruning_scaling/02b_nonstationary_scaling --config-name=base_dense
algorithm: grid

spec:
  metric: asymptotic_accuracy
  direction: minimize

plot_params:
  metrics: [asymptotic_loss, asymptotic_accuracy]
  split_by: [model.initial_hidden_units]

parameters:
  # Budgets {1x, 2x, ..., 64x} × per-unit cost 3176 = total weights.
  model.initial_hidden_units: [6, 12, 24, 48, 96, 192, 384]

  # Range mirrors 02_dense_scaling_4task_nonstationary_lr.
  optimizer.learning_rate:
    - "${eval:2**-11}"
    - "${eval:2**-10}"
    - "${eval:2**-9}"
    - "${eval:2**-8}"

  seed:
    type: atomic
    value: [0, 1, 2, 3, 4]
