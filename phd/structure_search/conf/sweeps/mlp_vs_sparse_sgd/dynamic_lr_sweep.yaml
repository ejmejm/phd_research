experiment: structure-search-sweeps
sweep_name: dynamic_sgd_lr_sweep
output_dir: outputs
optuna_storage: ${oc.env:OPTUNA_TRACKING_URI}
mlflow_storage: ${oc.env:MLFLOW_TRACKING_URI}
command: python train.py
algorithm: grid

# Match active connections to MLP (hidden_dim=64, n_layers=3): ~201K conns
# → dynamic 1598 units/layer × 32 conns + 1598×3×10 output = ~201K

spec:
  metric: asymptotic_loss
  direction: minimize

plot_params:
  metrics: [asymptotic_loss, asymptotic_accuracy]

parameters:
  mlflow: true
  log_individual_seeds: false

  model.type: dynamic
  model.n_layers: 3
  model.hidden_dim: 1598
  model.max_connections_per_unit: 32
  model.connect_all_to_output: true
  model.max_fan_out: 64

  optimizer.name: sgd

  optimizer.learning_rate:
    - "${eval:2**-9}"
    - "${eval:2**-7}"
    - "${eval:2**-5}"
    - "${eval:2**-3}"
    - "${eval:2**-1}"

  seed:
    type: atomic
    value: [1, 2, 3]
