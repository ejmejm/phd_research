# @package _global_
defaults:
  - /stationary
  - _self_

variant: utility_comparison
project: column-guided-utility-deep-dive-ns

model:
  n_layers: 1
  hidden_dim: 200
  max_units_per_layer: 3000

dataset:
  permute_period: 2000

structure_tracker:
  enabled: false
  connection_budget: 20_000
