# @package _global_
defaults:
  - /stationary
  - _self_

variant: no_column
project: column-guided-utility-deep-dive-ns

model:
  n_layers: 1
  hidden_dim: 175
  max_units_per_layer: 3000

dataset:
  permute_period: 2000

structure_tracker:
  connection_budget: 20_000
