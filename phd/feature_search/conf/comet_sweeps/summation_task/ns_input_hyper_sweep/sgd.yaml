name: sgd
project: ns_input_summation
command: python scripts/jax_full_feature_search.py --config-name=comet_sweeps/summation_task/base_config.yaml
algorithm: grid

spec:
  metric: cumulative_loss
  objective: minimize
  retryAssignLimit: 4

parameters:
  task.change_subset_freq: 200

  optimizer.name: sgd
  optimizer.learning_rate:
    - "${eval:2**-17}"
    - "${eval:2**-15}"
    - "${eval:2**-13}"
    - "${eval:2**-11}"
    - "${eval:2**-9}"
    - "${eval:2**-7}"
    - "${eval:2**-5}"
    - "${eval:2**-3}"

  seed: [301, 302, 303, 304, 305]