defaults:
  - rupam_task
  - _self_

model:
  shadow_dim: ${model.hidden_dim}

shadow_feature_recycling:
  use_cbp_utility: true
  utility_type: 'influence' # {cbp, age_normalized, influence}
  recycle_rate: 0.0 # 0.005
  utility_decay: 0.99
  feature_protection_steps: 0