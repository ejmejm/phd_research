defaults:
- full_feature_search
- _self_

model:
  ensemble_dim: 64
  n_ensemble_members: 16

  ensemble_feature_selection_method: random # {random, inverse_frequency}
  feature_utility_mode: mean # {mean, median, best}, could also try ensemble utility * feature utility in the future
  prediction_mode: mean # {mean, mean_top_half, median, median_top_half, best_ensemble}