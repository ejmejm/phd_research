defaults:
  - defaults

model:
  activation: tanh

# Task parameters
task:
  name: nonlinear_geoff
  type: regression
  n_features: 20
  n_real_features: 20
  flip_rate: 0.0
  n_layers: 2
  n_stationary_layers: 0 # Starts from the first layer
  hidden_dim: 20
  weight_scale: 1.0
  activation: tanh
  sparsity: 0.00
  weight_init: binary
